---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

This ephemeral resource generates a public/private key pair for signing notes. The keys are generated using the Go `sumdb/note` package's `GenerateKey` function, which creates keys compatible with the Go module checksum database format.

## Example Usage

{{ tffile "examples/ephemeral-resources/note-signer_keypair/ephemeral-resource.tf" }}

{{ .SchemaMarkdown | trimspace }}
